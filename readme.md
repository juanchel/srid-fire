### srid-fire

To run the app, install meteor, and run `meteor` from the directory. Or you can go to sridfire.meteor.com.

To use the app as a commander, sign up and log in as `admin`. Otherwise, you will be using the app as a client.
